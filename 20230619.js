// データの入力を受け付ける（prompt)
// 入力したした内容をそのまま表示するプログラムを作る
// let moji = prompt("入力せよ");
// console.log("入力したのは" + moji);
// 文字列「入力してください」を表示してユーザーに入力させた結果を、新規作成した変数mojiに入れろ。
// 文字列を「入力したのは」と変数mojiを連結した結果をコンソールに表示しろ。

// 数値と文字列を変換する
// let price = prompt("価格を入力せよ");
// console.log(price + 80);

// メソッドとオブジェクト
console.log("ハロー！", 10, 3.5);
// 文字列「ハロー！」と数値10と数値3.5をコンソールに表示しろ。

// chat GPT 演算子問題

// 1,比較演算子 "===" と "==" の違いは何ですか？
等価演算子
「==」比較する時に肩を自動変換する「数値123と文字列「123」の比例はtureを返します。(基本的には使わない)

「===」(厳密投下演算子)比較する値の型と値の両方を比較します。
左辺の値の型が数値であり、右辺の値の型が文字列は、値が等しいにも関わらず、型が異なるため結果は falseになる。
console.log(5 === 5);  // true
console.log("Hello" === "Hello");  // true
console.log(5 === "5");  // false
console.log(true === true);  // true
console.log(null === undefined);  // false

// 2,論理演算子 "&&" と "||" の働きは何ですか？
"&&" (かつ)   左右の値が両方ともTrueの時だけtrueを返します。「〇〇以上」かつ「〇〇以下」の２つの条件を組み合わせ.if文で判定。
let moji = prompt("年齢は？")
let age = parseInt(moji);
if(age >= 6 && age <=15) {
    console.log("義務教育の対象");
}
文字列「年齢は？」を表示してユーザーに入力させた結果を、新規作成した変数mojiに入れろ。
変数mojiを整数化して、新規作成した変数ageに入れろ。
もしも「変数ageが6以上、かつ変数ageが15以下」が真なら以下を実行せよ
{ 文字列「義務教育の対象」をコンソールに表示しろ。}

"||"(または)  左右のどちらかがtrueのときにtrueを返す。〇〇以上」または「〇〇以下」
let moji = prompt("年齢は？")
let age = parseInt(moji);
if(age <= 5 || age >= 65){
    console.log("幼児と高齢者");
}
文字列「年齢は？」を表示してユーザーに入力させた結果を、新規作成した変数mojiに入れろ。
変数mojiを整数化して、新規作成した変数ageに入れろ。
もしも「変数ageが5以下、または変数ageが65以上」が真なら以下を実行せよ
{ 文字列「幼児と高齢者」をコンソールに表示しろ。}


// 3,代入演算子 "=" と "==" の違いは何ですか？
"=" (等号): 代入演算子として使われます。右辺の値を左辺の変数に代入する

"==" (等価演算子): 比較演算子として使われます。左辺と右辺の値が等しいかどうかを比較する。

// 4,演算子 "!" はどのような働きをしますか？
"!"(ではない) trueとfalseを逆転します。真偽値を返す関数やメソッドの戻り値を逆転させたい場合にしよう。
let moji = prompt("年齢は？")
if( ! isNaN(moji)){
    let age = parseInt(moji);
    if(age < 18) {
        console.log("未成年")；
    }
}

// 5,インクリメント演算子 "++" とデクリメント演算子 "--" の使い方を教えてください。
インクリメント演算子 "++": 変数の値を1増加させます。
let x = 5;
x++; // x = x + 1 と同じ意味
console.log(x); // 結果: 6

デクリメント演算子 "--": 変数の値を1減少させます。
let y = 8;
y--; // y = y - 1 と同じ意味
console.log(y); // 結果: 7